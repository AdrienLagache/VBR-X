import useScrollTo from '../useScrollTo/useScrollTo';

import './Rossel.scss';

function Rossel(): JSX.Element {
  useScrollTo(0, 0);

  return (
    <section className="rossel">
      <div />
    </section>
  );
}

export default Rossel;
